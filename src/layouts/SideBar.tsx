import React from "react";

const SideBar = () => {
  return (
    <aside className="fixed top-0 bottom-0 w-0 flex-col bg-black text-white md:flex md:w-[64px] lg:w-[320px]">
      This is the left SideBar
    </aside>
  );
};

export default SideBar;
