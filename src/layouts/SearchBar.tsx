import React from "react";

const SearchBar = () => {
  return (
    <div className="fixed top-0 h-[64px] w-full border-2 border-dotted bg-light md:left-[64px] lg:left-[320px]">
      This is the top search bar
    </div>
  );
};

export default SearchBar;
